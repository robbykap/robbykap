name: pypi_build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [merged]

jobs:
  check-mdxcanvas-version:
    uses: ./.github/workflows/check-mdxcanvas.yaml

  build:
    needs: check-mdxcanvas-version
    runs-on: ubuntu-latest

    steps:
      - name: Install Poetry
        run: echo "hi"
